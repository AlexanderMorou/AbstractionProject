 /* ----------------------------------------------------------\
 |  This code was generated by Allen Copeland's Abstraction.  |
 |  Version: 0.5.0.0                                          |
 |------------------------------------------------------------|
 |  To ensure the code works properly,                        |
 |  please do not make any changes to the file.               |
 |------------------------------------------------------------|
 |  The specific language is C♯                               |
 |  Sub-tool Name: C♯ Code Translator                         |
 |  Sub-tool Version: 1.0.0.0                                 |
 \---------------------------------------------------------- */
using AllenCopeland.Abstraction.IO;
using AllenCopeland.Abstraction.Slf.Cli.Metadata;
using AllenCopeland.Abstraction.Slf.Cli.Metadata.Tables;
using System;
using System.IO;
namespace AllenCopeland.Abstraction.Slf._Internal.Cli.Metadata.Tables
{
    /// <summary>
    /// Provides a table which defines the offset to a standalone signature.
    /// </summary>
    /// <remarks>
    /// Used when a method is called by address, the standalone signature is pushed onto the
    /// stack and the call is made.
    /// </remarks>
    internal class CliMetadataStandAloneSigTableReader :
        CliMetadataLazyTable<ICliMetadataStandAloneSigTableRow>, 
        ICliMetadataTable, 
        ICliMetadataStandAloneSigTable
    {
        private ICliMetadataRoot metadataRoot;
        private EndianAwareBinaryReader reader;
        private object syncObject;
        private FileStream fStream;
        private uint rowCount;
        /// <summary>
        /// Size constant used when the total size of the <see cref="CliMetadataStandAloneSigLockedTableRow"/>
        /// is 2 bytes long.
        /// </summary>
        internal const int __COR_STANDALONESIG_CALC_SIZE_1__ = 2;
        /// <summary>
        /// Size constant used when the total size of the <see cref="CliMetadataStandAloneSigLockedTableRow"/>
        /// is 4 bytes long.
        /// </summary>
        internal const int __COR_STANDALONESIG_CALC_SIZE_2__ = 4;
        private int __size;
        private byte state;
        /// <summary>
        /// Data member which denotes where in the original stream the <see cref="CliMetadataStandAloneSigTableReader"/>
        /// is.
        /// </summary>
        private long streamOffset;
        private long length;
        private bool fullyRead;
        public override CliMetadataTableKinds Kind
        {
            get
            {
                return CliMetadataTableKinds.StandAloneSig;
            }
        }
        public long Length
        {
            get
            {
                return this.length;
            }
        }
        /// <summary>
        /// Initializes the <see cref="CliMetadataStandAloneSigTableReader"/> with the <paramref name="streamOffset"/>,<paramref name="blobHeapSize"/>
        /// provided.
        /// </summary>
        /// <param name="streamOffset">
        /// The <see cref="Int64"/> value which denotes where in the stream of <see cref="reader"/>
        /// the <see cref="CliMetadataStandAloneSigTableReader"/> is.
        /// </param>
        /// <param name="blobHeapSize">
        /// The <see cref="AllenCopeland.Abstraction.Slf.Cli.Metadata.Tables.CliMetadataReferenceIndexSize"/>
        /// used to denote the word size of the <see cref="AllenCopeland.Abstraction.Slf._Internal.Cli.Metadata.CliMetadataBlobHeaderAndHeap"/>.
        /// </param>
        internal void Initialize(long streamOffset, CliMetadataReferenceIndexSize blobHeapSize)
        {
            this.streamOffset = streamOffset;
            if (blobHeapSize == CliMetadataReferenceIndexSize.DWord)
            {
                this.state = 1;
                this.__size = CliMetadataStandAloneSigTableReader.__COR_STANDALONESIG_CALC_SIZE_2__;
            }
            else
            {
                this.state = 0;
                this.__size = CliMetadataStandAloneSigTableReader.__COR_STANDALONESIG_CALC_SIZE_1__;
            }
            this.length = this.__size * this.rowCount;
        }
        protected override ICliMetadataStandAloneSigTableRow ReadElementAt(uint index)
        {
            bool gotLock = false;
            System.Threading.Monitor.Enter(this.syncObject, ref gotLock);
            if (index == 0)
                return null;
            this.fStream.Seek(this.streamOffset + (index - 1) * this.__size, SeekOrigin.Begin);
            uint signatureIndex;
            switch (this.state)
            {
                case 1:
                    signatureIndex = this.reader.ReadUInt32();
                    break;
                default:
                    signatureIndex = this.reader.ReadUInt16();
                    break;
            }
            if (gotLock)
                System.Threading.Monitor.Exit(this.syncObject);
            return new CliMetadataStandAloneSigLockedTableRow(index, this.state, this.metadataRoot, signatureIndex);
        }
        public override void Read()
        {
            if (this.fullyRead)
                return;
            for (uint index = 1; index <= this.Count; index++)
            {
                bool gotLock = false;
                System.Threading.Monitor.Enter(this.syncObject, ref gotLock);
                if (!base.ItemLoaded(index))
                {
                    this.fStream.Seek(this.streamOffset + (index - 1) * this.__size, SeekOrigin.Begin);
                    uint signatureIndex;
                    switch (this.state)
                    {
                        case 1:
                            signatureIndex = this.reader.ReadUInt32();
                            break;
                        default:
                            signatureIndex = this.reader.ReadUInt16();
                            break;
                    }
                    base.InjectLoadedItem(new CliMetadataStandAloneSigLockedTableRow(index, this.state, this.metadataRoot, signatureIndex), index);
                }
                if (gotLock)
                    System.Threading.Monitor.Exit(this.syncObject);
            }
            this.fullyRead = true;
        }
        public CliMetadataStandAloneSigTableReader(ICliMetadataRoot metadataRoot, Tuple<object, FileStream, EndianAwareBinaryReader> readerInfo, uint rowCount)
            : base(metadataRoot, rowCount)
        {
            this.metadataRoot = metadataRoot;
            this.syncObject = readerInfo.Item1;
            this.fStream = readerInfo.Item2;
            this.reader = readerInfo.Item3;
            this.rowCount = rowCount;
        }
    };
};
